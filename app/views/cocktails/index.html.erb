<h1>My cocktails</h1>
<ul id="cocktails" style = "list-style: none">
  <% @cocktails.each do |cocktail| %>
    <li style = "text-decoration: none">
      <%= link_to cocktail.name, cocktail_path(cocktail)%>
    </li>
  <% end %>
</ul>

<% @cocktails.each do |cocktail| %>
 <%= link_to "#{cocktail.name}", new_cocktail_dose_path(cocktail, @dose) %>
<% end %>
